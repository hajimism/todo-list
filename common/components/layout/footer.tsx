import { p } from "@/styles/typography";

export const Footer = () => (
  <footer className="h-40 border-t bg-muted">
    <div className="flex h-full items-center justify-center">
      <p className={p()}>@hajimism</p>
    </div>
  </footer>
);
